# ── Nano Lab Secrets (TEMPLATE) ───────────────────────────────
# Copy to secrets.env and fill in real values. secrets.env is gitignored.
# Sourced by backup.sh and verify-backup.sh.

# Discord webhook for backup / verify notifications
BACKUP_WEBHOOK="https://discord.com/api/webhooks/XXXXXXXXX/XXXXXXXXXXXXXXXXXXXXXXXX"
